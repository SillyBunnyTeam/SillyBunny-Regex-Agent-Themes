# No trailing slash: during development this is a symlink into SillyBunny's tree,
# and a directory-only pattern would not match it.
node_modules
*.log
.DS_Store

# Gallery intermediates. Only the .webp files are committed; `--keep-png` writes the
# uncompressed captures here for inspection.
assets/themes/*.png
